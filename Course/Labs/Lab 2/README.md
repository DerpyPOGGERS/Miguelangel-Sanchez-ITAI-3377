I worked with Tensorflow and focused on testing a neural network mode. I trained the model using data and parameters like epochs. I learned how to improve a machine learning model by tuning it, such as changing the number of epochs or adjusting layers. One challenge was figuring out which changes would actually improve the model and which would make it worse. Sometimes the model would not perform as expected.
