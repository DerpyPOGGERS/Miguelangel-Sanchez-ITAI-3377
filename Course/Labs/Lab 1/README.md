In this lab, I worked with TensorFlow to build a simple neural network that can recognize handwritten numbers using the MNIST dataset. I learned how a basic neural network works from start to finish. Also how how models can be saved and converted into a smaller format for real-world applications like mobile or embedded systems. One difficulty was making sure the data was in the correct format so the model would run correctly.
